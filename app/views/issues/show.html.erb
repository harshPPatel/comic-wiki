<p>
  <strong>Name:</strong>
  <%= @issue.name %>
</p>
 
<p>
  <strong>On Comic Vine:</strong>
  <a href="<%= @issue.site_detail_url %>" target="_blank"><%= @issue.name %></a>
</p>

<p>
  <img src="<%= @issue.image_url %>">
</p>
 
<p>
  <strong>Description:</strong>
  <%= @issue.description.html_safe %>
</p>
 
<p>
  <strong>Characters:</strong>
</p>

<table>
  <tr>
    <th>ID</th>
    <th>Name</th>
    <th>Show Links</th>
  </tr>
  <% @issue.characters.each do |character| %>
    <tr>
      <td><%= character.id %></td>
      <td><%= character.name %></td>
      <td><%= link_to 'Show', character_path(character) %></td>
    </tr>
  <% end %>
</table>

<%= link_to 'Back' %>