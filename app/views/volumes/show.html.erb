<div class="jumbotron">
  <div class="row">
    <div class="col-4 image-container">
      <img src="<%= @volume.image_url %>" alt="<%= @volume.name %>">
    </div>
    <div class="col-8">
      <h1><%= @volume.name %></h1>
      <p class="lead"><%= @volume.description.html_safe %></p>
      <p><b>Start Year:</b> <%= @volume.start_year %></p>
      <a href="<%= @volume.site_detail_url %>" target="_blank" class="btn btn-info">Checkout on Comic Vine</a>
    </div>
  </div>
</div>

<h3 class="mb-3 mt-5">Volume issues:</h3>
<table class="table table-hover mb-5">
  <thead class="thead-dark">
    <tr>
      <th scope="col">ID</th>
      <th scope="col">Name</th>
      <th scope="col">Total Characters</th>
      <th scope="col">Link</th>
    </tr>
  </thead>
  <tbody>
    <% @volume.issues.each do |issue| %>
    <tr>
      <th scope="row"><%= issue.id %></th>
      <td><%= issue.name %></td>
      <td><%= issue.characters.length %></td>
      <td><%= link_to 'Show', issue_path(issue) %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'Back', :back, class: 'mt-3 mb-5 btn btn-dark' %>